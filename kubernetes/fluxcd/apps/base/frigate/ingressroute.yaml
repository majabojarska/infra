apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frigate
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`frigate.kube-01.home.majabojarska.dev`)
      kind: Rule
      services:
        - name: frigate
          port: 10095
    - match: Host(`frigate.home.majabojarska.dev`)
      kind: Rule
      services:
        - name: frigate
          port: 10095
  tls:
    secretName: certificate-secret
