apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingressroute-certificate
spec:
  secretName: certificate-secret
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - homepage.kube-01.home.majabojarska.dev
    - homepage.home.majabojarska.dev
