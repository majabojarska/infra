apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: searxng
spec:
  interval: 60m
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  url: oci://oci.trueforge.org/truecharts/searxng
  ref:
    tag: 10.14.0
