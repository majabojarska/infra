apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: esphome
spec:
  scaleTargetRef:
    name: esphome
  minReplicaCount: 0
  cooldownPeriod: 300
  triggers:
    - type: cron
      metadata:
        timezone: "Europe/Warsaw"
        start: "0 18 * * *"
        end: "0 1 * * *"
        desiredReplicas: "1"
